# 技术决策记录

## 2026-09-15：ACT 首屏聚焦促销异常诊断

- 促销异常的数值诊断由前端确定性引擎基于 POS 聚合结果计算；大模型只解释计算结果。
- 候选动作固定为停止促销、改会员券、维持折扣、补货/调拨，并先经过证据和数据完整性规则。
- 未接入实时库存、缺货时长和在途数量时，补货/调拨候选必须拒绝；审批只是记录试验授权，不触发生产执行。
- 回放指标区分原因判断代理指标、动作有效率和无效建议率；因果准确率需要后续处理组/对照组数据才能升级为正式指标。
- 推演只比较有限候选动作，并用透明的 7 天销售/毛利区间表达假设；单月 POS 不足以支持精确的反事实因果数字。
- 店长输入采用“一句话命题”模式；系统在零售场景自动加载学清路店 POS 口径、训练/盲测窗口和报告字段，复杂约束不要求店长手动填写。

## 2026-09-15：零售产品重构为单店决策双胞胎

- 首个可落地场景锁定为“学清路店 · 一周经营决策”，先完整实现促销异常诊断，再扩展到主推、陈列、补货和排班。
- MiroFish 只在事实层和规则层限定的有限动作集合内推演经营影响，不负责产生业务事实或自由扩展世界设定。
- 报告采用固定结构，严格分开训练期事实、动作假设和盲测回放；回放打脸的动作不得继续作为确定性推荐。
- 产品正确性拆为计算正确、证据可追溯、动作可执行和业务效果可验证四类指标；单月 POS 只用于回放和受控试验设计，不承诺复杂因果或收益预测。

## 2026-09-16：学清路店促销策略场景包保留 MiroFish 工作流

- 保留 WHAT IF、SIMULATE、OBSERVE、INTERVIEW 和 ACT 的原有交互框架；零售促销问题改由数据场景包建立初始角色、关系和三轮有限推演，避免让通用 LLM 生成零售事实。
- 场景包只绑定版本化 POS 快照的口径、角色、动作和验证能力，原始数据继续由后端接口提供，不嵌入前端配置。
- 报告是每次推演的固定输出；ACT 和回放是可选的“决策后续与验证”能力。历史盲测只在验证章节使用，未来真实场景可改用运营结果回流、持续监测或补充证据。
- 当前 POS 缺少促销机制主数据，因此只比较公开折扣、会员券和维持策略；满减、买赠、第二件半价等动作必须等待促销类型和成本分摊数据接入。

## 2026-09-16：训练期 POS 作为零售世界先验，模型推演作为可证伪假设

- 不对基础模型做微调；系统将训练期 POS 压缩为版本化的世界先验，包含门店与商品事实、已证据化关系、未知项和硬边界，并在每次实体构建和每轮推演时注入模型上下文。
- 训练期 POS 决定事实角色和初始关系；模型可补充假设角色和关系，但每条关系必须带假设文本、置信度、证据引用与证伪条件，不能生成或修改销量、毛利、库存、客群规模和促销机制事实。
- 促销场景不再走固定三轮或固定报告的短路路径：模型完成多角色推演和报告；规则引擎仍限定可执行动作，防止假设直接越过约束进入执行。
- 用户记录历史回放后，回放结论以校正记忆保存；后续推演读取该记忆以降低或修正不被支持的行为关系。盲测始终不进入事前世界先验。

## 2026-09-16：盲测只评分提前声明的趋势，不伪造反事实因果验证

- 模型必须在盲测数据不可见时，为可观测关系声明验证指标和方向；当前可验证的指标限定为到店日均销售、到店日均毛利、毛利率、会员订单占比和促销销售占比。
- 报告生成并封存后，系统才读取 24–30 日数据，输出“趋势吻合、趋势不吻合、无法验证”。评分不自动进入世界记忆，运营人员确认后才回流。
- 该评分只说明预测方向与后来观察是否一致，不证明“改会员券”等策略带来因果效果；没有真实执行记录的反事实策略必须保留为待试验假设。

## 2026-09-16：店长报告与验证界面分离

- 报告页只呈现当前行动建议、行动依据、责任人、观察指标和停止条件；不展示日期、训练窗口、盲测、回放、数据处理过程或模型过程。
- 历史验证和世界记忆仍保留在 ACT，不进入报告正文或生成后的决策文案。
- 零售报告页移除首屏图谱概览和 KPI 图，顶部收缩为单行工具栏；正文区域独立滚动，优先服务阅读行动建议。
- 店长版本最多展示三项行动；每项只保留“做什么、为什么、谁做、什么情况停”，推演细节和完整试验字段留在其他工作台，不进入决策阅读路径。
- 阅读顺序固定为“先做这三件事”置顶，再展示简短建议说明，最后展开证据、责任人、观察指标和停止条件；行动意见优先于方法和依据。

## 2026-09-16：移除模型生成的 KPI 预测曲线

- 原 KPI 曲线由模型按每轮图谱状态给出 0–1 归一化分数，缺少真实口径和可验证性，不得用于零售经营判断。
- 已删除其模型调用、随机演示数据、状态保存、导出与展示入口；保留图谱节点和关系数量曲线，因为它反映真实的图谱结构变化。

## 2026-09-16：报告使用固定三段式，避免模型 JSON 大纲失败

- 店长报告的结构固定为“先做什么、为什么做、怎么执行”；模型只生成每段的短文本，不再承担 JSON 大纲生成任务。
- 这不限制模型的推演或建议内容，只消除了大纲输出被截断时导致整份报告失败的脆弱点。
- 报告采用“短行动清单 + 三段判断说明”的层次：置顶行动清单保持极简，后续每段固定解释判断、依据与风险/停止条件，避免只剩口号或退回长篇分析。

## 2026-09-16：学清路店作为独立零售生态场景

- 通用“零售 · 单店生态”不再绑定任何特定门店的 POS、促销动作或店长命题，保留用于开放的零售世界推演。
- 新增“零售 · 学清路生态”场景包，承载学清路店的 POS 数据边界、促销沙盘、世界先验、店长命题和 ACT 作战台规则。
- 推荐 WHAT IF 问题只在学清路生态中出现，并在输入后自动加载该场景的数据；通用零售不会意外引用学清路事实或生成基于该数据的动作。

## 2026-09-16：模型选择促销动作，规则只负责约束

- 促销规则保留有限候选池和否决条件，但不再按固定分数或候选顺序自动产出推荐。
- 每轮报告完成前，模型必须从规则已放行的候选中选择最多三项动作；选择结果经候选 ID 校验后才进入报告和 ACT。
- ACT 只展示本轮推演选择的待审批动作；候选池明确标注为规则范围，不再把预设动作或静态七天区间当作系统建议。
- 报告只在顶部展示一次行动清单，后续展示“为什么做、怎么执行、何时停止”和对应的落实条件，避免同一动作重复出现。
